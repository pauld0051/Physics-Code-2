const velocityAnswer = document.getElementById("si_velocity_answer");
function siAnswer(evt) {
  if (event.target.id === "si_m") { // event.target is the changed element
    velocityAnswer.textContent = (distance / time).toFixed(3);
  }
}

const form = document.querySelector("form");
document.getElementById("resetButton").addEventListener("reset", resetForms);
function resetForms() {
  document.getElementById("kinematicCheckBox").reset();
  document.getElementById("suvat_calc").reset();
  form.lastElementChild.disabled = false;
}
